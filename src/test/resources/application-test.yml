spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/datetime_test_db
    username: root
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop   # creates schema for each test run
    show-sql: true
